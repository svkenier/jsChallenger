// Escribir una función que tome una matriz de cadenas como argumento
// Ordenar alfabéticamente los elementos de la matriz
// Devuelve el resultado

const sortAlphabeticallyArray = (arr) => {
  return arr.sort();
};

console.log(sortAlphabeticallyArray([ "oso","uva","tambor","ala"]));

//comentario uso de el metodo sort() para clasificar el array alfabeticamente