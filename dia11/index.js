// Escribir una función que tome un objeto con dos propiedades como argumento
// Debe devolver el valor de la propiedad con clave país

const showCountry = (obj) => {
  return obj.country;
};
console.log(toShowCountry({ continent: "Asia", country: "Japon" }));

//comentario 1: funcion muy simple que retorna la propieda de el objeto con
//codigo ciudada
